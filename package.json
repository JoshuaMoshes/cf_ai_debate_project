{
  "name": "ai-debate-coach",
  "version": "1.0.0",
  "description": "AI Debate Coach on Cloudflare",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "serve": "python3 -m http.server 8080 --directory web"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0"
  }
}